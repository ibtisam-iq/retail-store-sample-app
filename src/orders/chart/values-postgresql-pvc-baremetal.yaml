# Deployed the orders service with in-cluster PostgreSQL and a PVC on bare-metal.
# Used local-path StorageClass (Rancher) — data persisted across pod restarts.

app:
  persistence:
    provider: postgresql
    secret:
      password: "orders123"
  messaging:
    provider: in-memory

postgresql:
  create: true
  persistentVolume:
    enabled: true
    size: 10Gi
    storageClass: local-path
