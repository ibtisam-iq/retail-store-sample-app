# Scenario: DB=in-memory (H2), Messaging=in-memory
# Used to verify the chart renders correctly with zero infrastructure dependencies.

app:
  persistence:
    provider: 'in-memory'

  messaging:
    provider: 'in-memory'
