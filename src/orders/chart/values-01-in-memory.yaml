# Deployed the orders service fully in-memory: H2 embedded DB + in-memory messaging.
# Baseline scenario — no PostgreSQL, no RabbitMQ, no SQS. Fastest startup, zero dependencies.

app:
  persistence:
    provider: in-memory
  messaging:
    provider: in-memory
