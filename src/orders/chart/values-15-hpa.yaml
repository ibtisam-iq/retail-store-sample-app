# Scenario: HPA enabled — horizontal pod autoscaling based on CPU utilization
# Combine with any DB/messaging overlay using multiple -f flags.

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
