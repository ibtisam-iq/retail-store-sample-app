# Scenario: DB=PostgreSQL + gp3 EBS PVC, Messaging=RabbitMQ + gp3 EBS PVC (EKS)
# Both DB and message queue use EBS gp3 volumes on EKS; full persistent stack without managed services.

app:
  persistence:
    provider: postgres
    secret:
      password: "orders123"

  messaging:
    provider: rabbitmq
    rabbitmq:
      secret:
        username: orders
        password: "orders123"

postgresql:
  create: true
  persistentVolume:
    enabled: true
    size: 1Gi
    storageClass: gp3

rabbitmq:
  create: true
  persistentVolume:
    enabled: true
    size: 1Gi
    storageClass: gp3
