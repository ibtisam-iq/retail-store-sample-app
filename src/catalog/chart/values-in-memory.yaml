# Deployed the catalog service with in-memory persistence — no external database.
# Used as the baseline to verify the chart rendered and the pod came up healthy.
# No keys differ from values.yaml defaults — this file confirms in-memory is the default behavior.

app:
  persistence:
    provider: in-memory
